/**
 * Firebase SDK 생성기 모듈
 *
 * Firebase JS SDK의 API를 C# + jslib 브릿지로 자동 생성합니다.
 * 출력 디렉토리: Runtime/Firebase/ (기존 Runtime/SDK/와 분리)
 */

export { generateFirebaseSDK } from './FirebaseSDKGenerator.js';
